// Copy this file to app_config.dart and confirm values before deployment.
// app_config.dart is intentionally ignored because deployment values may be secret.

const bool debug = false;
const Map<String, dynamic> configItems = {
  'appTitle': 'stundaa',
  'default_language_code': 'en',
};
const String baseUrl = 'https://stund.id/';
const String baseApiUrl = 'https://stund.id/api/';
const String publicKey = '';
